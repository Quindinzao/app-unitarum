import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    button: {
        justifyContent: 'flex-end'
    },
    container: {
        height: '100%',
        width: '100%'
    },
    header: {
        justifyContent: 'flex-end',
        marginBottom: 0
    }
})